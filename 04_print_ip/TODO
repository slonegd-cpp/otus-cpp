(+-) Тесты есть и они лаконичны и по делу.
Но добавьте тесты на vector<list<чисел>> vector<list<string>> и т.д.
Иначе из тестов не понятно, такие конструкции вообще разрешены или нет.
И если разрешены, то что от них ожидать.
(Лучше конечно запретить такие конструкции если в задании явно это не требуется,
но можно и оставить - это ваш, как разработчика, выбор)
Добавьте тест на разнородный tuple. Но такой код, похоже,
у вас не должен компилировался, как тогда проверить?
Гляньте вот это: https://goo.gl/6TJNm8 
(-) В main не ловите исключения. А памяти может не хватить даже для такой простой программы.
(-) https://github.com/slonegd/print_ip-otus04-/blob/master/print_ip.h#L46L57
Написано в комментарии, что это для контейнеров целых чисел, но не где не проверяется,
что T::value_type - целое число. Что будет для vector<string>?
(-) Будет ли ваш код правильно работать на системе с обратным порядком байт?
(-) Старайтесь не использовать итераторы явно
(если вы конечно не пишите какой-то новый алгоритм, где их использование уместно)
https://github.com/slonegd/print_ip-otus04-/blob/master/print_ip.h#L53